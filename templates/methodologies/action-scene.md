# Action Scene

Use the role-specific action methodology named by the injected prompt.

Action-scene state lives in Glass MCP tools and neutral graph facts. Public prose is submitted only through `glass_turn_append(body="...")`.
