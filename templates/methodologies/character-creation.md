# Character Creation

Use the role-specific character-creation methodology named by the injected prompt.

The shared rule is simple: character data goes into `glass_character_*` MCP tools and neutral graph facts. Public prose goes only through `glass_turn_append(body="...")`. No files are authored during agent turns.
