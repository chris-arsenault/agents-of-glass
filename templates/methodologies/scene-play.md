# Scene Play

Use the role-specific scene-play methodology named by the injected prompt.

Scene continuity lives in Glass MCP tools and neutral graph facts. Public prose is submitted only through `glass_turn_append(body="...")`.
