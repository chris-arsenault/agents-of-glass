---
title: Output Contract
target: executing-agent
authority: binding
---

# Output Contract

Write your final public turn prose to the path named in `TURN_START.md` and
exit.

Do not put YAML, JSON, analysis notes, private planning, or tool transcripts in
that file. The orchestrator reads only the turn output file for public prose.

The public turn can include OOC player voice and IC character narration when
both matter. Keep those registers clear in prose; do not add a schema for them.

If you make rolls or state changes during the turn, narrate the visible outcome
in the public prose. The `glass` commands leave their own audit trail; your
turn prose is the story moment that other agents and viewers read.

When an instruction or methodology says to run a `glass` command, execute that
command in the shell during your turn. Do not write `glass ...` command lines
into the public prose file as a substitute for executing them.
